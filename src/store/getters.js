let getters = {
    bfoot: (state) => { return state.bfoot } , //别忘了return回去
    bloadding: (state) => { return state.bloadding },
    banner: (state) => state.banner,
    dujia: (state) => state.dujia,
    news: (state) => state.news,
    shop: (state) => state.shop,
    car: (state) => state.car,
    
}

export default getters